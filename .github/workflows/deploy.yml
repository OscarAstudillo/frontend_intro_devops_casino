name: CI/CD Pipeline Casino Frontend

on:
  push:
    branches:
      - deploy

env:
  ECR_REPOSITORY: casino-frontend

jobs:
  build-and-push:
    name: Construir y subir imagen a Amazon ECR
    runs-on: ubuntu-latest
    steps:
      - name: 1. Checkout del código
        uses: actions/checkout@v4

      - name: 2. Configurar credenciales de AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: 3. Iniciar sesión en Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: 4. Construir, etiquetar y publicar la imagen
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:latest -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG -t $ECR_REGISTRY/$ECR_REPOSITORY:v1.0.1 .
          docker push -a $ECR_REGISTRY/$ECR_REPOSITORY

  deploy:
    name: Desplegar en EC2 Frontend
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Desplegar vía SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.AWS_HOST_IP_FRONTEND }}
          username: ec2-user
          key: ${{ secrets.AWS_SSH_KEY }}
          script: |
            aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 590183930707.dkr.ecr.us-east-1.amazonaws.com
            docker pull 590183930707.dkr.ecr.us-east-1.amazonaws.com/casino-frontend:latest
            docker rm -f casino_frontend || true
            docker run -d \
              --name casino_frontend \
              -p 80:8080 \
              -e BACKEND_HOST=${{ secrets.AWS_BACKEND_PRIVATE_IP }} \
              --restart unless-stopped \
              590183930707.dkr.ecr.us-east-1.amazonaws.com/casino-frontend:latest